### 标注说明

本工具是 [ok-dna](https://github.com/BnanZ0/ok-duet-night-abyss) 的配套标注档案库，用于管理和处理标注数据。

#### 标注流程：
1. 运行 `DEBUG.py` 启动项目。
2. 在侧边导航栏找到并点击 **“模版”** 选项卡进入标注页面。
3. 完成标注后，点击 **“保存”** 按钮。
4. 在弹出的选项中选择 **“(assets)独立应用”** 并确认。
5. 系统将自动执行压缩逻辑，并将处理后的文件更新至 `assets/images` 目录下。